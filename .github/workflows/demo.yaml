name: demo

on:
  workflow_dispatch:

env:
  DAYS_OF_MONTH: 30
  
jobs:
  call-workflow-passing-data:
    uses: ./.github/workflows/new.yaml
    with:
      personal-name: Ahmed Salah
    secrets:
      envPAT: ${{ secrets.envPAT }}
  demo:
    runs-on: ubuntu
    needs: call-workflow-passing-data
    steps:
      - run: echo ${{ needs.call-workflow-passing-data.outputs.token }}
