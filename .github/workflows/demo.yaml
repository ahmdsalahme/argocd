name: "my work flow"
on:
  workflow_dispatch: {}
permissions:
  contents: write
jobs:
  job1:
    runs-on: ubuntu-latest
    steps:
    - name: List
      run: echo "${{ toJSON(github) }}"
