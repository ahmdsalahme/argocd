name: "Validate Branch Name"

on:
  pull_request:
    branches: ["main"]

jobs:
  validate-branch:
    runs-on: ubuntu-latest
    steps:
    - name: List Branches
      run: echo "The pull request branch name is ${{ github.head_ref }}"